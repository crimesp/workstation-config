#have these two lines to include the bash helpers etc in this project
#append this running the following from this directory
#cat .bash_aliases >> $USER_HOME/.bash_aliases

unalias -a
source $BASH_INCLUDES/.bashrc_extensions
